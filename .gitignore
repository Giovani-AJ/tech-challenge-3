# Segredos/artefatos locais que nunca devem ser versionados.
*.env
.env
token.txt
*.pem
*.key

# Binários compilados localmente (ex: `go build` sem -o em services/*)
services/*/*.exe
services/*/*-service
